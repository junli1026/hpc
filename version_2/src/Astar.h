#include "roomy.h"
#include "RoomyHashTable.h"

uint64 dest;
uint64 start;
uint64 Lev;



// user define here
#define MIN 0
#define MAX 100

extern uint64 hFunc(uint64 a);
extern uint64 gFunc(uint64 b);
